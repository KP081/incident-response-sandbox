# Remediation: auth-service

**Action:** rollback_deployment

Rolling back deployment build 4471 on auth-service to revert the incorrect key rotation in prod-rotated.pem that caused JWT signature verification failures and login rejections ("JWT signature verification failed: key mismatch after rotation" and "login rejected for user 88213: invalid token").
