# Remediation: auth-service

**Action:** rollback_deployment

Rollback auth-service from build 4471 to revert the bad key rotation (prod-rotated.pem causing JWT signature verification failures) and restore successful token verification.
