# Remediation: auth-service

**Action:** rollback_deployment

Rolling back deployment build 4471 on auth-service to revert bad JWT key rotation ('prod-rotated.pem') causing signature verification failures.
